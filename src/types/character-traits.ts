import type { CharacterTrait } from './character-trait';

export interface CharacterTraits {
  speed: CharacterTrait;
  might: CharacterTrait;
  sanity: CharacterTrait;
  knowledge: CharacterTrait;
}
